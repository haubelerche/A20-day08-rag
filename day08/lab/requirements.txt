# Day 08 Lab — RAG Pipeline
# Cài đặt: pip install -r requirements.txt

# === Core ===
python-dotenv>=1.0.0

# === LLM Providers (chọn ít nhất một) ===
openai>=1.0.0                    # Option A: OpenAI API
google-generativeai>=0.5.0       # Option B: Google Gemini

# === Vector Store ===
chromadb>=0.5.0                  # Vector database (local, không cần server)

# === Embeddings — chọn một trong hai ===
# Option A: OpenAI Embeddings (dùng chung API key với LLM)
# (đã có trong openai package)

# Option B: Sentence Transformers (local, không cần API key)
sentence-transformers>=2.7.0     # Model: paraphrase-multilingual-MiniLM-L12-v2

# === Sprint 3: Sparse/Hybrid Retrieval ===
rank-bm25>=0.2.2                 # BM25 keyword search

# === Sprint 3: Rerank ===
# sentence-transformers đã bao gồm CrossEncoder

# === Sprint 4: Evaluation ===
ragas>=0.1.0                     # RAGAS evaluation framework (faithfulness, relevancy, recall)
datasets>=2.0.0                  # HuggingFace datasets — dependency của RAGAS 0.1.x

# === Frontend ===
chainlit>=1.0.0                  # Chat UI (chạy: chainlit run app.py -w)

# === Utils ===
tqdm>=4.66.0                     # Progress bar khi index nhiều docs
